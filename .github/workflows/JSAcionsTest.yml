name: javascript actions test

on:
  workflow_dispatch

jobs:
  GitHub-Actions:
    runs-on: ubuntu-latest

    steps:
      - name: JS action call
        uses: unmeshtemkar/JSActionWithoutNodeModule@d16cc29bb4d81176500cbe05cab7162a0491e2db
        with:
          who-to-greet: 'Testing with normal index.js and node_modules directory"'
