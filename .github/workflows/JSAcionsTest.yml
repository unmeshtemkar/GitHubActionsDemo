name: javascript actions test

on:
  workflow_dispatch

jobs:
  GitHub-Actions:
    runs-on: ubuntu-latest

    steps:
      - name: JS action call
        uses: unmeshtemkar/JSActionWithoutNodeModule@v1.0
        with:
          who-to-greet: 'Unmesh'
