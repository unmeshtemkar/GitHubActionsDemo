name: javascript actions test

on:
  workflow_dispatch

jobs:
  GitHub-Actions:
    runs-on: ubuntu-latest

    steps:
      - name: JS action call
        uses: unmeshtemkar/JSActionWithoutNodeModule@68bdea45dbf9f15bb4a130ac31e73257b896a8b0
        with:
          who-to-greet: 'Testing with normal index.js and removed node_modules directory"'
